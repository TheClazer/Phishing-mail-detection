// Vitest setup file — runs before each test file.
// Currently empty; reserved for global test mocks (e.g. ResizeObserver polyfill)
// when we add component tests that need DOM APIs jsdom doesn't ship.
